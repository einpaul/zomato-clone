<%= form_for(@restaurant_type) do |f| %>
  <% if @restaurant_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant_type.errors.count, "error") %> prohibited this restaurant_type from being saved:</h2>

      <ul>
      <% @restaurant_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
    <%= f.submit %>
  </div>
<% end %>
